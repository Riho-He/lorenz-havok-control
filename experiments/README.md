# Experiments

Runnable experiments and generated outputs for the HAVOK control project.

Each experiment folder should contain:

- `README.md`: question, method, command, and conclusion
- `run.py`: main script
- `config.json`: experiment parameters
- `results/`: metrics, figures, and tables
