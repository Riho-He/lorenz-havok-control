# Data

Data used or generated by the HAVOK control experiments.

Use `raw/` for original data files and `processed/` for cleaned or derived data.

Large generated caches should usually stay out of git unless they are necessary for reproducing a result.
